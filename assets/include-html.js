const $dataAtribute = document.querySelectorAll("[data-include]");

console.log($dataAtribute);
/*document.addEventListener("DOMContentLoaded", async (e) => {
    try {


        //el.outerHTML = $html



        let res = await axios.get(`${$dataAtributte}`),
            $html = await res.data;

    } catch (err) {
        let message = err.statusText || "Error al cargar el archivo, verifica que estes haciendo peticion por http o https";
        let $paragraph = d.createElement("p");
        $paragraph.innerHTML = `${err.status}: ${message}`;
        $dataAtribute.outerHTML = $paragraph;
    }
    const includeHTML = (el, url) => {
    }

});*/